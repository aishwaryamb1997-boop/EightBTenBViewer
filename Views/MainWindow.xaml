<Window x:Class="EightBTenBViewer.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="EightBTenBViewer" Height="450" Width="800">
<DockPanel>
    <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="8,6,8,4">
        <Button Content="Export CSV" Padding="10,4" Click="ExportCsv_Click"/>
    </StackPanel>

    <DataGrid ItemsSource="{Binding Records}" AutoGenerateColumns="False">

    <DataGrid.Columns>

    <DataGridTextColumn Header="SL" Binding="{Binding SerialNo}" Width="50"/>
    <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp}" Width="150"/>

    <!-- L0 -->
    <DataGridTemplateColumn Header="L0" Width="240">
        <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
                <ItemsControl ItemsSource="{Binding Lanes[0].Symbols}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="50"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Bits10}" Grid.Column="0"/>
                                <TextBlock Text="{Binding Type}" Grid.Column="1" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Hex}" Grid.Column="2" HorizontalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </DataTemplate>
        </DataGridTemplateColumn.CellTemplate>
    </DataGridTemplateColumn>

    <!-- L1 -->
    <DataGridTemplateColumn Header="L1" Width="240">
        <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
                <ItemsControl ItemsSource="{Binding Lanes[1].Symbols}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="50"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Bits10}" Grid.Column="0"/>
                                <TextBlock Text="{Binding Type}" Grid.Column="1" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Hex}" Grid.Column="2" HorizontalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </DataTemplate>
        </DataGridTemplateColumn.CellTemplate>
    </DataGridTemplateColumn>

    <!-- L2 -->
    <DataGridTemplateColumn Header="L2" Width="240">
        <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
                <ItemsControl ItemsSource="{Binding Lanes[2].Symbols}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="50"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Bits10}" Grid.Column="0"/>
                                <TextBlock Text="{Binding Type}" Grid.Column="1" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Hex}" Grid.Column="2" HorizontalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </DataTemplate>
        </DataGridTemplateColumn.CellTemplate>
    </DataGridTemplateColumn>

    <!-- L3 -->
    <DataGridTemplateColumn Header="L3" Width="240">
        <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
                <ItemsControl ItemsSource="{Binding Lanes[3].Symbols}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="50"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Bits10}" Grid.Column="0"/>
                                <TextBlock Text="{Binding Type}" Grid.Column="1" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Hex}" Grid.Column="2" HorizontalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </DataTemplate>
        </DataGridTemplateColumn.CellTemplate>
    </DataGridTemplateColumn>

    <!-- L4 -->
    <DataGridTemplateColumn Header="L4" Width="240">
        <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
                <ItemsControl ItemsSource="{Binding Lanes[4].Symbols}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="50"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Bits10}" Grid.Column="0"/>
                                <TextBlock Text="{Binding Type}" Grid.Column="1" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Hex}" Grid.Column="2" HorizontalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </DataTemplate>
        </DataGridTemplateColumn.CellTemplate>
    </DataGridTemplateColumn>

    <!-- L5 -->
    <DataGridTemplateColumn Header="L5" Width="240">
        <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
                <ItemsControl ItemsSource="{Binding Lanes[5].Symbols}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="50"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Bits10}" Grid.Column="0"/>
                                <TextBlock Text="{Binding Type}" Grid.Column="1" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Hex}" Grid.Column="2" HorizontalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </DataTemplate>
        </DataGridTemplateColumn.CellTemplate>
    </DataGridTemplateColumn>

    <!-- L6 -->
    <DataGridTemplateColumn Header="L6" Width="240">
        <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
                <ItemsControl ItemsSource="{Binding Lanes[6].Symbols}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="50"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Bits10}" Grid.Column="0"/>
                                <TextBlock Text="{Binding Type}" Grid.Column="1" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Hex}" Grid.Column="2" HorizontalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </DataTemplate>
        </DataGridTemplateColumn.CellTemplate>
    </DataGridTemplateColumn>

    <!-- L7 -->
    <DataGridTemplateColumn Header="L7" Width="240">
        <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
                <ItemsControl ItemsSource="{Binding Lanes[7].Symbols}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="50"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Bits10}" Grid.Column="0"/>
                                <TextBlock Text="{Binding Type}" Grid.Column="1" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Hex}" Grid.Column="2" HorizontalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </DataTemplate>
        </DataGridTemplateColumn.CellTemplate>
    </DataGridTemplateColumn>

    <!-- L8 -->
    <DataGridTemplateColumn Header="L8" Width="240">
        <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
                <ItemsControl ItemsSource="{Binding Lanes[8].Symbols}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="50"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Bits10}" Grid.Column="0"/>
                                <TextBlock Text="{Binding Type}" Grid.Column="1" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Hex}" Grid.Column="2" HorizontalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </DataTemplate>
        </DataGridTemplateColumn.CellTemplate>
    </DataGridTemplateColumn>

    <!-- L9 -->
    <DataGridTemplateColumn Header="L9" Width="240">
        <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
                <ItemsControl ItemsSource="{Binding Lanes[9].Symbols}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="50"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Bits10}" Grid.Column="0"/>
                                <TextBlock Text="{Binding Type}" Grid.Column="1" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Hex}" Grid.Column="2" HorizontalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </DataTemplate>
        </DataGridTemplateColumn.CellTemplate>
    </DataGridTemplateColumn>

    <!-- L10 -->
    <DataGridTemplateColumn Header="L10" Width="240">
        <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
                <ItemsControl ItemsSource="{Binding Lanes[10].Symbols}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="50"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Bits10}" Grid.Column="0"/>
                                <TextBlock Text="{Binding Type}" Grid.Column="1" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Hex}" Grid.Column="2" HorizontalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </DataTemplate>
        </DataGridTemplateColumn.CellTemplate>
    </DataGridTemplateColumn>

    <!-- L11 -->
    <DataGridTemplateColumn Header="L11" Width="240">
        <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
                <ItemsControl ItemsSource="{Binding Lanes[11].Symbols}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="50"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Bits10}" Grid.Column="0"/>
                                <TextBlock Text="{Binding Type}" Grid.Column="1" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Hex}" Grid.Column="2" HorizontalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </DataTemplate>
        </DataGridTemplateColumn.CellTemplate>
    </DataGridTemplateColumn>

    <!-- L12 -->
    <DataGridTemplateColumn Header="L12" Width="240">
        <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
                <ItemsControl ItemsSource="{Binding Lanes[12].Symbols}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="50"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Bits10}" Grid.Column="0"/>
                                <TextBlock Text="{Binding Type}" Grid.Column="1" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Hex}" Grid.Column="2" HorizontalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </DataTemplate>
        </DataGridTemplateColumn.CellTemplate>
    </DataGridTemplateColumn>

    <!-- L13 -->
    <DataGridTemplateColumn Header="L13" Width="240">
        <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
                <ItemsControl ItemsSource="{Binding Lanes[13].Symbols}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="50"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Bits10}" Grid.Column="0"/>
                                <TextBlock Text="{Binding Type}" Grid.Column="1" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Hex}" Grid.Column="2" HorizontalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </DataTemplate>
        </DataGridTemplateColumn.CellTemplate>
    </DataGridTemplateColumn>

    <!-- L14 -->
    <DataGridTemplateColumn Header="L14" Width="240">
        <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
                <ItemsControl ItemsSource="{Binding Lanes[14].Symbols}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="50"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Bits10}" Grid.Column="0"/>
                                <TextBlock Text="{Binding Type}" Grid.Column="1" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Hex}" Grid.Column="2" HorizontalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </DataTemplate>
        </DataGridTemplateColumn.CellTemplate>
    </DataGridTemplateColumn>

    <!-- L15 -->
    <DataGridTemplateColumn Header="L15" Width="240">
        <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
                <ItemsControl ItemsSource="{Binding Lanes[15].Symbols}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="50"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Bits10}" Grid.Column="0"/>
                                <TextBlock Text="{Binding Type}" Grid.Column="1" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Hex}" Grid.Column="2" HorizontalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </DataTemplate>
        </DataGridTemplateColumn.CellTemplate>
    </DataGridTemplateColumn>

    </DataGrid.Columns>
    </DataGrid>
</DockPanel>
</Window>
